# Apopka Seniors training hub — static site behind server-side Basic Auth.
#
# Replace the hash below with your own:
#   docker run --rm caddy caddy hash-password --plaintext 'your-password-here'
# Paste the resulting string after the username.

:80 {
	root * /srv
	encode gzip

	basic_auth {
		volunteer $2b$14$jOuWuR4yewChUXTAXChSvOK6fP3UdwPeKsgRebvwVZ/jd3FDflNiG
	}

	file_server

	header {
		X-Content-Type-Options nosniff
		X-Frame-Options DENY
		Referrer-Policy no-referrer
		-Server
	}
}
